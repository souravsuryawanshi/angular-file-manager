import { Providers, Components, Imports } from './declaration';
import { NgModule } from "@angular/core";

@NgModule({
  imports : [Imports],
  providers : [Providers],
  declarations : [Components]
})

export class FolderScreenModule{}
